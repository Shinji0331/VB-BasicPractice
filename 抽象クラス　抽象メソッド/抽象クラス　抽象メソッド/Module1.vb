Module Module1

    Sub Main()
        Dim NotePC As New Notebook()
        Dim DesktopPC As New Desktop()

        NotePC.DispComputerType()
        DesktopPC.DispComputerType()

        Console.ReadLine()

    End Sub

End Module
